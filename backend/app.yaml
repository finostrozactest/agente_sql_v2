# Ruta: backend/app.yaml
# --- VERSIÓN FINAL DEFINITIVA ---
service: backend
runtime: python311
instance_class: F4  # Aumentamos la memoria

# NO hay 'secret_env_variables'. El código se encarga de ello.

entrypoint: gunicorn -b :$PORT main:app -w 4 -k uvicorn.workers.UvicornWorker

automatic_scaling:
  min_instances: 0
  max_instances: 1
